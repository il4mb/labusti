<Window xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"  x:Name="window" x:Class="LaborHost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LaborHost"
        xmlns:lib="clr-namespace:LaborHost.Lib"
        mc:Ignorable="d"
        Title="Laboratorium Host" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        </Style>
    </Window.Resources>
    <Grid Height="{Binding ActualWidth, ElementName=window, Mode=OneWay}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding Width, ElementName=sidebar}"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <lib:Sidebar x:Name="sidebar"></lib:Sidebar>

        <lib:ClientContainer Grid.Column="1"/>
    </Grid>
</Window>
